import React from 'react'

/**
 * @fileoverview This file contains the page component that serves as the main entry point for the messages page.
 * It is a server component that displays a list of opened conversations of the user.
 * The conversationId is passed as a parameter to the page component.
 */
const page = () => {
    return (
        <div>page</div>
    )
}

export default page